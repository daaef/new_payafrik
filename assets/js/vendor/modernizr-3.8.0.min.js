/*! modernizr 3.8.0 (Custom Build) | MIT *
 * https://modernizr.com/download/?-cssanimations-csscolumns-customelements-flexbox-history-picture-pointerevents-postmessage-sizes-srcset-webgl-websockets-webworkers-addtest-domprefixes-hasevent-mq-prefixedcssvalue-prefixes-setclasses-testallprops-testprop-teststyles ! */
!(function (e, t, n) {
  function r(e, t) {
    return typeof e === t
  }
  function o(e) {
    let t = b.className
    const n = Modernizr._config.classPrefix || ''
    if ((S && (t = t.baseVal), Modernizr._config.enableJSClass)) {
      const r = new RegExp('(^|\\s)' + n + 'no-js(\\s|$)')
      t = t.replace(r, '$1' + n + 'js$2')
    }
    Modernizr._config.enableClasses &&
      (e.length > 0 && (t += ' ' + n + e.join(' ' + n)),
      S ? (b.className.baseVal = t) : (b.className = t))
  }
  function i(e, t) {
    if (typeof e === 'object') for (const n in e) P(e, n) && i(n, e[n])
    else {
      e = e.toLowerCase()
      const r = e.split('.')
      let s = Modernizr[r[0]]
      if ((r.length === 2 && (s = s[r[1]]), void 0 !== s)) return Modernizr
      ;(t = typeof t === 'function' ? t() : t),
        r.length === 1
          ? (Modernizr[r[0]] = t)
          : (!Modernizr[r[0]] ||
              Modernizr[r[0]] instanceof Boolean ||
              (Modernizr[r[0]] = new Boolean(Modernizr[r[0]])),
            (Modernizr[r[0]][r[1]] = t)),
        o([(t && !1 !== t ? '' : 'no-') + r.join('-')]),
        Modernizr._trigger(e, t)
    }
    return Modernizr
  }
  function s() {
    return typeof t.createElement !== 'function'
      ? t.createElement(arguments[0])
      : S
      ? t.createElementNS.call(t, 'http://www.w3.org/2000/svg', arguments[0])
      : t.createElement.apply(t, arguments)
  }
  function a() {
    let e = t.body
    return e || ((e = s(S ? 'svg' : 'body')), (e.fake = !0)), e
  }
  function l(e, n, r, o) {
    let i
    let l
    let u
    let f
    const c = 'modernizr'
    const d = s('div')
    const p = a()
    if (parseInt(r, 10))
      for (; r--; )
        (u = s('div')), (u.id = o ? o[r] : c + (r + 1)), d.appendChild(u)
    return (
      (i = s('style')),
      (i.type = 'text/css'),
      (i.id = 's' + c),
      (p.fake ? p : d).appendChild(i),
      p.appendChild(d),
      i.styleSheet
        ? (i.styleSheet.cssText = e)
        : i.appendChild(t.createTextNode(e)),
      (d.id = c),
      p.fake &&
        ((p.style.background = ''),
        (p.style.overflow = 'hidden'),
        (f = b.style.overflow),
        (b.style.overflow = 'hidden'),
        b.appendChild(p)),
      (l = n(d, e)),
      p.fake
        ? (p.parentNode.removeChild(p), (b.style.overflow = f), b.offsetHeight)
        : d.parentNode.removeChild(d),
      !!l
    )
  }
  function u(e, t) {
    return !!~('' + e).indexOf(t)
  }
  function f(e) {
    return e
      .replace(/([A-Z])/g, function (e, t) {
        return '-' + t.toLowerCase()
      })
      .replace(/^ms-/, '-ms-')
  }
  function c(t, n, r) {
    let o
    if ('getComputedStyle' in e) {
      o = getComputedStyle.call(e, t, n)
      const i = e.console
      if (o !== null) r && (o = o.getPropertyValue(r))
      else if (i) {
        const s = i.error ? 'error' : 'log'
        i[s].call(
          i,
          'getComputedStyle returning null, its possible modernizr test results are inaccurate'
        )
      }
    } else o = !n && t.currentStyle && t.currentStyle[r]
    return o
  }
  function d(t, r) {
    let o = t.length
    if ('CSS' in e && 'supports' in e.CSS) {
      for (; o--; ) if (e.CSS.supports(f(t[o]), r)) return !0
      return !1
    }
    if ('CSSSupportsRule' in e) {
      for (var i = []; o--; ) i.push('(' + f(t[o]) + ':' + r + ')')
      return (
        (i = i.join(' or ')),
        l(
          '@supports (' + i + ') { #modernizr { position: absolute; } }',
          function (e) {
            return c(e, null, 'position') === 'absolute'
          }
        )
      )
    }
    return n
  }
  function p(e) {
    return e
      .replace(/([a-z])-([a-z])/g, function (e, t, n) {
        return t + n.toUpperCase()
      })
      .replace(/^-/, '')
  }
  function m(e, t, o, i) {
    function a() {
      f && (delete L.style, delete L.modElem)
    }
    if (((i = !r(i, 'undefined') && i), !r(o, 'undefined'))) {
      const l = d(e, o)
      if (!r(l, 'undefined')) return l
    }
    for (
      var f, c, m, h, A, v = ['modernizr', 'tspan', 'samp'];
      !L.style && v.length;

    )
      (f = !0), (L.modElem = s(v.shift())), (L.style = L.modElem.style)
    for (m = e.length, c = 0; c < m; c++)
      if (
        ((h = e[c]),
        (A = L.style[h]),
        u(h, '-') && (h = p(h)),
        L.style[h] !== n)
      ) {
        if (i || r(o, 'undefined')) return a(), t !== 'pfx' || h
        try {
          L.style[h] = o
        } catch (e) {}
        if (L.style[h] !== A) return a(), t !== 'pfx' || h
      }
    return a(), !1
  }
  function h(e, t) {
    return function () {
      return e.apply(t, arguments)
    }
  }
  function A(e, t, n) {
    let o
    for (const i in e)
      if (e[i] in t)
        return !1 === n
          ? e[i]
          : ((o = t[e[i]]), r(o, 'function') ? h(o, n || t) : o)
    return !1
  }
  function v(e, t, n, o, i) {
    const s = e.charAt(0).toUpperCase() + e.slice(1)
    let a = (e + ' ' + z.join(s + ' ') + s).split(' ')
    return r(t, 'string') || r(t, 'undefined')
      ? m(a, t, o, i)
      : ((a = (e + ' ' + x.join(s + ' ') + s).split(' ')), A(a, t, n))
  }
  function g(e, t, r) {
    return v(e, n, n, t, r)
  }
  const y = []
  const C = {
    _version: '3.8.0',
    _config: {
      classPrefix: '',
      enableClasses: !0,
      enableJSClass: !0,
      usePrefixes: !0,
    },
    _q: [],
    on(e, t) {
      const n = this
      setTimeout(function () {
        t(n[e])
      }, 0)
    },
    addTest(e, t, n) {
      y.push({ name: e, fn: t, options: n })
    },
    addAsyncTest(e) {
      y.push({ name: null, fn: e })
    },
  }
  var Modernizr = function () {}
  ;(Modernizr.prototype = C), (Modernizr = new Modernizr())
  const w = []
  var b = t.documentElement
  var S = b.nodeName.toLowerCase() === 'svg'
  const _ = 'Moz O ms Webkit'
  var x = C._config.usePrefixes ? _.toLowerCase().split(' ') : []
  C._domPrefixes = x
  const T = C._config.usePrefixes
    ? ' -webkit- -moz- -o- -ms- '.split(' ')
    : ['', '']
  C._prefixes = T
  let P
  !(function () {
    const e = {}.hasOwnProperty
    P =
      r(e, 'undefined') || r(e.call, 'undefined')
        ? function (e, t) {
            return t in e && r(e.constructor.prototype[t], 'undefined')
          }
        : function (t, n) {
            return e.call(t, n)
          }
  })(),
    (C._l = {}),
    (C.on = function (e, t) {
      this._l[e] || (this._l[e] = []),
        this._l[e].push(t),
        Modernizr.hasOwnProperty(e) &&
          setTimeout(function () {
            Modernizr._trigger(e, Modernizr[e])
          }, 0)
    }),
    (C._trigger = function (e, t) {
      if (this._l[e]) {
        const n = this._l[e]
        setTimeout(function () {
          let e
          for (e = 0; e < n.length; e++) (0, n[e])(t)
        }, 0),
          delete this._l[e]
      }
    }),
    Modernizr._q.push(function () {
      C.addTest = i
    })
  const k = (function () {
    function e(e, r) {
      let o
      return (
        !!e &&
        ((r && typeof r !== 'string') || (r = s(r || 'div')),
        (e = 'on' + e),
        (o = e in r),
        !o &&
          t &&
          (r.setAttribute || (r = s('div')),
          r.setAttribute(e, ''),
          (o = typeof r[e] === 'function'),
          r[e] !== n && (r[e] = n),
          r.removeAttribute(e)),
        o)
      )
    }
    var t = !('onblur' in b)
    return e
  })()
  C.hasEvent = k
  const E = (function () {
    const t = e.matchMedia || e.msMatchMedia
    return t
      ? function (e) {
          const n = t(e)
          return (n && n.matches) || !1
        }
      : function (t) {
          let n = !1
          return (
            l(
              '@media ' + t + ' { #modernizr { position: absolute; } }',
              function (t) {
                n =
                  (e.getComputedStyle
                    ? e.getComputedStyle(t, null)
                    : t.currentStyle
                  ).position === 'absolute'
              }
            ),
            n
          )
        }
  })()
  C.mq = E
  const B = function (e, t) {
    let n = !1
    const r = s('div')
    const o = r.style
    if (e in o) {
      let i = x.length
      for (o[e] = t, n = o[e]; i-- && !n; )
        (o[e] = '-' + x[i] + '-' + t), (n = o[e])
    }
    return n === '' && (n = !1), n
  }
  C.prefixedCSSValue = B
  var z = C._config.usePrefixes ? _.split(' ') : []
  C._cssomPrefixes = z
  const O = { elem: s('modernizr') }
  Modernizr._q.push(function () {
    delete O.elem
  })
  var L = { style: O.elem.style }
  Modernizr._q.unshift(function () {
    delete L.style
  }),
    (C.testAllProps = v),
    (C.testAllProps = g)
  ;(C.testProp = function (e, t, r) {
    return m([e], n, t, r)
  }),
    (C.testStyles = l)
  Modernizr.addTest('customelements', 'customElements' in e),
    Modernizr.addTest('history', function () {
      const t = navigator.userAgent
      return (
        !!t &&
        ((!t.includes('Android 2.') && !t.includes('Android 4.0')) ||
          !t.includes('Mobile Safari') ||
          t.includes('Chrome') ||
          t.includes('Windows Phone') ||
          location.protocol === 'file:') &&
        e.history &&
        'pushState' in e.history
      )
    })
  const N = [''].concat(x)
  ;(C._domPrefixesAll = N),
    Modernizr.addTest('pointerevents', function () {
      for (let e = 0, t = N.length; e < t; e++)
        if (k(N[e] + 'pointerdown')) return !0
      return !1
    })
  let R = !0
  try {
    e.postMessage(
      {
        toString() {
          R = !1
        },
      },
      '*'
    )
  } catch (e) {}
  Modernizr.addTest('postmessage', new Boolean('postMessage' in e)),
    Modernizr.addTest('postmessage.structuredclones', R),
    Modernizr.addTest('webgl', function () {
      return 'WebGLRenderingContext' in e
    })
  let j = !1
  try {
    j = 'WebSocket' in e && e.WebSocket.CLOSING === 2
  } catch (e) {}
  Modernizr.addTest('websockets', j),
    Modernizr.addTest('cssanimations', g('animationName', 'a', !0)),
    (function () {
      Modernizr.addTest('csscolumns', function () {
        let e = !1
        const t = g('columnCount')
        try {
          ;(e = !!t), e && (e = new Boolean(e))
        } catch (e) {}
        return e
      })
      for (
        var e,
          t,
          n = [
            'Width',
            'Span',
            'Fill',
            'Gap',
            'Rule',
            'RuleColor',
            'RuleStyle',
            'RuleWidth',
            'BreakBefore',
            'BreakAfter',
            'BreakInside',
          ],
          r = 0;
        r < n.length;
        r++
      )
        (e = n[r].toLowerCase()),
          (t = g('column' + n[r])),
          (e !== 'breakbefore' && e !== 'breakafter' && e !== 'breakinside') ||
            (t = t || g(n[r])),
          Modernizr.addTest('csscolumns.' + e, t)
    })(),
    Modernizr.addTest('flexbox', g('flexBasis', '1px', !0)),
    Modernizr.addTest('picture', 'HTMLPictureElement' in e),
    Modernizr.addAsyncTest(function () {
      let e
      let t
      let n
      const r = s('img')
      const o = 'sizes' in r
      !o && 'srcset' in r
        ? ((t =
            'data:image/gif;base64,R0lGODlhAgABAPAAAP///wAAACH5BAAAAAAALAAAAAACAAEAAAICBAoAOw=='),
          (e =
            'data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw=='),
          (n = function () {
            i('sizes', r.width === 2)
          }),
          (r.onload = n),
          (r.onerror = n),
          r.setAttribute('sizes', '9px'),
          (r.srcset = e + ' 1w,' + t + ' 8w'),
          (r.src = e))
        : i('sizes', o)
    }),
    Modernizr.addTest('srcset', 'srcset' in s('img')),
    Modernizr.addTest('webworkers', 'Worker' in e),
    (function () {
      let e, t, n, o, i, s, a
      for (const l in y)
        if (y.hasOwnProperty(l)) {
          if (
            ((e = []),
            (t = y[l]),
            t.name &&
              (e.push(t.name.toLowerCase()),
              t.options && t.options.aliases && t.options.aliases.length))
          )
            for (n = 0; n < t.options.aliases.length; n++)
              e.push(t.options.aliases[n].toLowerCase())
          for (
            o = r(t.fn, 'function') ? t.fn() : t.fn, i = 0;
            i < e.length;
            i++
          )
            (s = e[i]),
              (a = s.split('.')),
              a.length === 1
                ? (Modernizr[a[0]] = o)
                : ((Modernizr[a[0]] &&
                    (!Modernizr[a[0]] || Modernizr[a[0]] instanceof Boolean)) ||
                    (Modernizr[a[0]] = new Boolean(Modernizr[a[0]])),
                  (Modernizr[a[0]][a[1]] = o)),
              w.push((o ? '' : 'no-') + a.join('-'))
        }
    })(),
    o(w),
    delete C.addTest,
    delete C.addAsyncTest
  for (let M = 0; M < Modernizr._q.length; M++) Modernizr._q[M]()
  e.Modernizr = Modernizr
})(window, document)
